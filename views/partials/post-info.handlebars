<article class="post2">
  <div class="title">
    <a href="{{post_url}}" target="_blank">{{title}}</a>
    <span>({{format_url post_url}})</span>
  </div>
  <div class="meta">
    |
    <a href="/post/{{id}}">{{comments.length}} {{format_plural "Feedback" comments.length}}</a>
    <button class="status-section">Status : pending </button>  
    </div>
</article>